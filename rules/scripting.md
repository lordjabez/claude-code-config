# Scripting

Run bash commands one at a time when possible to avoid unnecesary permissions requests.

If a complex command or set of commands is needed, write them to a script file in the
local folder, run that, then delete it. The same goes for one-off Python scripts.
